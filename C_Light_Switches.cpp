#include <bits/stdc++.h>
using namespace std;

int main() {
       int t;
       cin>>t;
       while(t--){
              int n,k;
              cin>>n>>k;
              vector<long long> arr(n,0);
              long long start=LONG_LONG_MIN;
              for(int i=0;i<n;i++){
                     cin>>arr[i];
                     m=max(m,arr[i]);
              }
              long long end=start+k-1;
              long long ans=-1;
              

              

       }
       return 0;
}