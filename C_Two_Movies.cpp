#include <bits/stdc++.h>
using namespace std;

int main() {
       int t;
       cin>>t;
       while(t--)
       {
              int n;
              cin>>n;
              vector<int> frst(n,0);
              vector<int> frst(n,0);
              for(int i=0;i<n;i++)
              {
                     cin>>frst[i];
              }
              for(int i=0;i<n;i++)
              {
                     cin>>scnd[i];
              }
       }
       return 0;
}